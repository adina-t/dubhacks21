//"use strict";

//init();

/*function init(){
  let collapsible = document.getElementsByClassName("collapse");
  for( let i = 0; i < 3; i++) {
    collapsible[i].addEventListener("click", function () {
      let options = document.querySelectorAll(".content");
      this.classList.toggle("activate");
      if(options[i].style.display === "none"){
        content.style.display = inline;
      } else {
        content.style.display = none;
      }
    }
  }

*/
/*
const typeToInputId = new Map([
  [microwaves, "food-3"],
  [cafes, "food-1"],
  [restaurants, "food-2"],
  [bikeStorages, "transportation1"],
  [busStops, "transportation2"],
  [studySpaces, "name-space1"]
]);*/
/*
const typeCheckboxes = document.querySelectorAll("input[type='checkbox']");
typeCheckboxes.forEach(addEventListener( 'change', function() {
    if(this.checked) {
      document.querySelectorAll([inputType=this.id]).setVisible(true);

    } else {
      document.querySelectorAll([inputType=this.id]).setVisible(false);
    }
}));*/


